[project]
name = "lad-python"
version = "0.1.0"
description = "Add your description here"
authors = [
    { name = "Julia Gorb", email = "ulia.gor.2010@yandex.ru" }
]
requires-python = ">=3.14"
dependencies = [
    "ruff>=0.16.8",
]

[project.scripts]
lad-python = "lad_python:main"

[build-system]
requires = ["uv_build>=0.12.17,<0.13.0"]
build-backend = "uv_build"
